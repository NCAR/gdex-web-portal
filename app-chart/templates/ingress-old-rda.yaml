apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-old-rda
  namespace: {{ .Release.Namespace }}
  labels:
    group: {{ .Values.webapp.group }}
  annotations:
    cert-manager.io/cluster-issuer: "incommon"
    nginx.ingress.kubernetes.io/rewrite-target: https://rda.ucar.edu$1$2
spec:
  ingressClassName: nginx-internal
  tls:
    - hosts:
      - rda.k8s.ucar.edu
      secretName: incommon-cert.rda.k8s.ucar.edu
  rules:
  - host: rda.k8s.ucar.edu
    http:
      paths:
      - path: /([^/]*)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: service-dummy
            port:
              number: 80
